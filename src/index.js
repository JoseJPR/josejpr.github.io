/** Import main dependencies */
import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Instance ReactDOM render method.
ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
